# Implementation Plans

Generated by the improve skill on 2026-07-16. Execute in the order below. Each executor must read the plan fully, honor its STOP conditions, and update the status row unless a reviewer is maintaining this index.

## Execution order and status

| Plan | Title                                  | Priority | Effort | Depends on | Status |
| ---- | -------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Remove test and documentation ceremony | P1       | M      | —          | DONE   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- Plan 001 is self-contained and has no dependencies.
- Plan 001 was implemented and independently verified at commit `30e7daf`.

## Findings considered and rejected

- Extract a shared fake-dependencies factory: not worth doing because it adds a production-adjacent abstraction to remove ordinary composition duplication; colocating the one-consumer harness removes the pressure.
- Convert service classes to free functions: not worth doing because the classes cleanly retain injected dependencies and the change is mostly syntax churn.
- Remove service-layer pass-through reads: rejected because it would make HTTP depend directly on stores or require a new exception to the repository's dependency direction.
- Share a logger factory between production and tests: rejected because it couples CloudWatch emission behavior to test-capture needs for negligible reduction.
- Split infrastructure tests from the default suite: rejected because roughly 400 ms saved from an approximately two-second suite does not justify extra scripts/configuration, and the tests protect production-only security and retention behavior.
- Remove router tests: rejected because path extraction, URL decoding, method dispatch, and wrong-depth rejection are cheap and not fully characterized by endpoint tests.
- Remove decorator tests wholesale: rejected because malformed bearer parsing, opaque `500` responses, request-scoped context, and log behavior protect distinct security and observability contracts.
- Collapse ports, fake adapters, or decorators into fewer files: rejected because each has multiple implementations or a distinct responsibility; fewer files would not reduce behavior or maintenance cost.
- Deduplicate domain status literals into exported runtime tuples: deferred because it adds public runtime constants and is unrelated to the requested simplicity cut.
